---
phase: 04-fragility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProtocolTriage.jsx
  - src/components/HistoryPage.jsx
  - src/components/TriageDetailsModal.jsx
autonomous: true
requirements: [FRAG-01, FRAG-04]
must_haves:
  truths:
    - "A non-2xx response from /protocol_names surfaces an error in the catch block instead of silently passing bad data to res.json()"
    - "A non-2xx response from /transcription logs console.error but does NOT throw, alert, or interrupt the triage flow"
    - "A non-2xx response from /protocol-traverse (other than 503 retry) throws an error that reaches the user"
    - "After a PDF is opened via window.open, the blob URL is revoked after 60 seconds in all three download sites"
  artifacts:
    - path: "src/components/ProtocolTriage.jsx"
      provides: "5 new response.ok guards + 1 blob URL revocation"
      contains: "revokeObjectURL"
    - path: "src/components/HistoryPage.jsx"
      provides: "blob URL revocation after PDF download"
      contains: "revokeObjectURL"
    - path: "src/components/TriageDetailsModal.jsx"
      provides: "blob URL revocation after PDF download"
      contains: "revokeObjectURL"
  key_links:
    - from: "ProtocolTriage.jsx protocol_names fetch"
      to: "existing .catch(err => console.error(...))"
      via: "new !res.ok throw inside .then() chain before res.json()"
      pattern: "if.*!res\\.ok.*throw"
    - from: "ProtocolTriage.jsx traverseTree 503 check"
      to: "new !response.ok guard"
      via: "guard placed AFTER 503 continue block, BEFORE response.json()"
      pattern: "if.*!response\\.ok.*throw"
---

<objective>
Add response.ok guards to the 5 unguarded fetch calls in ProtocolTriage.jsx and add blob URL revocation to all 3 PDF download sites.

Purpose: Non-2xx responses currently pass silently through fetch calls that skip status checks, and blob URLs accumulate in memory during long triage sessions.
Output: Hardened fetch calls and memory-safe PDF downloads across ProtocolTriage.jsx, HistoryPage.jsx, and TriageDetailsModal.jsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fragility/04-CONTEXT.md
@.planning/phases/04-fragility/04-RESEARCH.md

<interfaces>
<!-- ProtocolTriage.jsx fetch sites requiring guards (line numbers from current code): -->

<!-- Site 1: protocol_names .then() chain (lines 335-346) — user-visible per D-04 -->
getAuthHeaders().then(headers => {
    fetch(`${API_URL}/protocol_names`, { headers })
        .then(res => res.json())  // <-- NO ok check before .json()
        .then(data => { ... })
        .catch(err => console.error("Error fetching protocols:", err));
});

<!-- Site 2: transcription in handlePatientSubmit (line 387) — silent per D-03 -->
await fetch(`${API_URL}/transcription`, { method: 'POST', headers, body });
// No ok check at all

<!-- Site 3: transcription in logSystemMessage (line 415) — silent per D-03 -->
await fetch(`${API_URL}/transcription`, { method: 'POST', headers, body });
// No ok check at all

<!-- Site 4: transcription in handleSendMessage (line 451) — silent per D-03 -->
fetch(`${API_URL}/transcription`, { method: 'POST', headers, body })
    .catch(e => console.error("Transcription upload failed", e));
// No ok check at all

<!-- Site 5: protocol-traverse in traverseTree (line 629) — user-visible per D-04/D-05 -->
const response = await fetch(`${API_URL}/protocol-traverse`, { ... });
clearTimeout(timeoutId);
if (response.status === 503 && attempt < MAX_RETRIES) { ... continue; }
const data = await response.json();  // <-- NO ok check between 503 block and .json()

<!-- Blob URL sites requiring revokeObjectURL: -->

<!-- HistoryPage.jsx:30 -->
const url = window.URL.createObjectURL(blob);
window.open(url, '_blank');
// No revocation

<!-- TriageDetailsModal.jsx:124 -->
const url = window.URL.createObjectURL(blob);
window.open(url, '_blank');
// No revocation

<!-- ProtocolTriage.jsx:838 -->
const url = window.URL.createObjectURL(blob);
window.open(url, '_blank');
// No revocation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add response.ok guards to 5 unguarded fetch calls in ProtocolTriage.jsx</name>
  <files>src/components/ProtocolTriage.jsx</files>
  <read_first>
    - src/components/ProtocolTriage.jsx (full file — executor must see current line numbers and control flow)
    - .planning/phases/04-fragility/04-CONTEXT.md (decisions D-01 through D-05)
    - .planning/phases/04-fragility/04-RESEARCH.md (code examples and pitfall warnings)
  </read_first>
  <action>
    Add `response.ok` guards to 5 fetch call sites in ProtocolTriage.jsx. Two behavior categories per D-03/D-04:

    **User-visible errors (throw on failure):**

    1. `GET /protocol_names` (useEffect around line 335-346): Inside the `.then()` chain, change `.then(res => res.json())` to `.then(res => { if (!res.ok) throw new Error('Erro ao carregar protocolos: ' + res.status); return res.json(); })`. The existing `.catch(err => console.error(...))` will handle the thrown error. Keep the `.then()` chain style — do NOT refactor to async/await (per D-04 and research pitfall #4).

    2. `POST /protocol-traverse` (traverseTree function, around line 629-642): Insert `if (!response.ok) { throw new Error('Erro no protocolo de triagem: ' + response.status); }` AFTER the `if (response.status === 503 && attempt < MAX_RETRIES) { ... continue; }` block and BEFORE `const data = await response.json();`. CRITICAL: the new guard must come AFTER the 503 check, not before it — otherwise 503 retries break (per D-05 and research pitfall #1).

    **Silent errors (console.error only, never throw):**

    3. `POST /transcription` in handlePatientSubmit (around line 387-394): After the `await fetch(...)` call, add: `if (!transcriptionRes.ok) console.error('Transcription context log failed:', transcriptionRes.status);` — assign the fetch result to a variable `transcriptionRes` if not already assigned. Do NOT throw here — the patient info was already registered successfully (per D-03).

    4. `POST /transcription` in logSystemMessage (around line 415-422): After `await fetch(...)`, capture the response and add: `const res = await fetch(...); if (!res.ok) console.error('Transcription system log failed:', res.status);` — this is inside its own try/catch so a silent console.error is sufficient (per D-03).

    5. `POST /transcription` in handleSendMessage (around line 451-458): This uses a `.then()/.catch()` chain. Add ok check: `fetch(...).then(res => { if (!res.ok) console.error('Transcription upload failed:', res.status); }).catch(e => console.error("Transcription upload failed", e));` (per D-03).
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && grep -n "response\.ok\|res\.ok\|!res\.ok\|!response\.ok" src/components/ProtocolTriage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "!res.ok" src/components/ProtocolTriage.jsx returns at least 2 (protocol_names + handleSendMessage transcription)
    - grep -c "!response.ok" src/components/ProtocolTriage.jsx returns at least 2 (protocol-traverse + existing patientResponse guard)
    - grep -c "!transcriptionRes.ok" src/components/ProtocolTriage.jsx returns at least 1 (handlePatientSubmit transcription)
    - grep -n "Erro ao carregar protocolos" src/components/ProtocolTriage.jsx returns a match (protocol_names error message)
    - grep -n "Erro no protocolo de triagem" src/components/ProtocolTriage.jsx returns a match (protocol-traverse error message)
    - The line with "if (!response.ok)" for protocol-traverse appears AFTER the line with "response.status === 503" (verify with grep -n)
    - npm run build succeeds with no errors
  </acceptance_criteria>
  <done>All 5 previously-unguarded fetch calls now check response.ok. /protocol_names and /protocol-traverse throw on failure. /transcription calls log console.error only. The 503 retry loop is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add blob URL revocation to all 3 PDF download sites</name>
  <files>src/components/ProtocolTriage.jsx, src/components/HistoryPage.jsx, src/components/TriageDetailsModal.jsx</files>
  <read_first>
    - src/components/ProtocolTriage.jsx (see handleDownloadPDF around line 828-846)
    - src/components/HistoryPage.jsx (see handleDownloadPDF around line 20-37)
    - src/components/TriageDetailsModal.jsx (see handleDownloadPDF around line 115-132)
    - .planning/phases/04-fragility/04-CONTEXT.md (decisions D-12, D-13, D-14)
  </read_first>
  <action>
    Add `setTimeout(() => URL.revokeObjectURL(url), 60000);` after each `window.open(url, '_blank');` call in all 3 PDF download handlers. The pattern is identical per D-14:

    1. **HistoryPage.jsx** (around line 30-31): After `window.open(url, '_blank');` add `setTimeout(() => URL.revokeObjectURL(url), 60000);`

    2. **TriageDetailsModal.jsx** (around line 124-125): After `window.open(url, '_blank');` add `setTimeout(() => URL.revokeObjectURL(url), 60000);`

    3. **ProtocolTriage.jsx** (around line 838-839): After `window.open(url, '_blank');` add `setTimeout(() => URL.revokeObjectURL(url), 60000);`

    CRITICAL: Do NOT call revokeObjectURL synchronously — the browser needs time to load the PDF in the opened tab. The 60-second delay is required per D-13 and research pitfall #2.
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && grep -n "revokeObjectURL" src/components/HistoryPage.jsx src/components/TriageDetailsModal.jsx src/components/ProtocolTriage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "revokeObjectURL" src/components/HistoryPage.jsx returns 1
    - grep -c "revokeObjectURL" src/components/TriageDetailsModal.jsx returns 1
    - grep -c "revokeObjectURL" src/components/ProtocolTriage.jsx returns 1
    - grep "setTimeout.*revokeObjectURL.*60000" src/components/HistoryPage.jsx returns a match
    - grep "setTimeout.*revokeObjectURL.*60000" src/components/TriageDetailsModal.jsx returns a match
    - grep "setTimeout.*revokeObjectURL.*60000" src/components/ProtocolTriage.jsx returns a match
    - npm run build succeeds with no errors
  </acceptance_criteria>
  <done>All 3 PDF download handlers revoke blob URLs after 60 seconds. No blob URLs accumulate in memory during long sessions.</done>
</task>

</tasks>

<verification>
1. `grep -n "!res.ok\|!response.ok\|!transcriptionRes.ok" src/components/ProtocolTriage.jsx` — shows all new guards
2. `grep -n "revokeObjectURL" src/components/HistoryPage.jsx src/components/TriageDetailsModal.jsx src/components/ProtocolTriage.jsx` — shows revocation in all 3 files
3. `npm run build` — no build errors
</verification>

<success_criteria>
- Every fetch call in ProtocolTriage.jsx has a response.ok guard (5 new + existing ones)
- /transcription failures log to console.error only, never alert or throw
- /protocol_names and /protocol-traverse failures throw errors that surface to user
- 503 retry loop in traverseTree remains functional
- All 3 blob URL sites call URL.revokeObjectURL after 60s delay
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-fragility/04-01-SUMMARY.md`
</output>
