---
phase: 07-component-migration-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TriageDetailsModal.jsx
  - src/components/TriageDetailsModal.css
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02

must_haves:
  truths:
    - "TriageDetailsModal renders with token-backed CSS classes and zero inline styles"
    - "Modal has role='dialog', aria-modal='true', aria-labelledby pointing to title"
    - "Focus is trapped inside modal when open"
    - "Focus returns to trigger element when modal closes"
    - "No failing contrast hex values remain"
  artifacts:
    - path: "src/components/TriageDetailsModal.css"
      provides: "Token-backed CSS for TriageDetailsModal"
      min_lines: 80
    - path: "src/components/TriageDetailsModal.jsx"
      provides: "Migrated modal with ARIA attributes and focus trap"
  key_links:
    - from: "src/components/TriageDetailsModal.jsx"
      to: "src/components/TriageDetailsModal.css"
      via: "CSS import and className references"
      pattern: "import.*TriageDetailsModal\\.css"
    - from: "src/components/TriageDetailsModal.jsx"
      to: "aria-labelledby"
      via: "modal title ID"
      pattern: "aria-labelledby"
---

<objective>
Migrate TriageDetailsModal inline styles and add modal accessibility (ARIA + focus trap).

Purpose: Eliminates 71 inline styles from the 507-line modal component, adds `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and implements native DOM focus trapping so keyboard users cannot tab outside the modal. This is the most complex single-component migration (heavy styling + ARIA + focus management).
Output: TriageDetailsModal.css with token-backed styles. TriageDetailsModal.jsx with zero inline styles and full ARIA compliance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-component-migration-accessibility/07-CONTEXT.md
@.planning/phases/07-component-migration-accessibility/07-UI-SPEC.md
@.planning/phases/07-component-migration-accessibility/07-RESEARCH.md

<interfaces>
From src/styles/tokens.css (semantic tokens to use):
```css
[data-app-theme] {
  --color-primary: var(--color-teal-500);
  --color-surface: #ffffff;
  --color-surface-subtle: var(--color-gray-50);
  --color-surface-muted: var(--color-gray-100);
  --color-border: var(--color-gray-200);
  --color-border-strong: var(--color-gray-300);
  --color-text-primary: var(--color-gray-900);
  --color-text-secondary: var(--color-gray-600);
}
```

From src/App.css (priority badge classes — reuse for modal priority display):
```css
.priority-badge { display: inline-block; padding: 1rem 2rem; border-radius: 8px; font-weight: bold; color: white; }
.priority-red    { background-color: var(--mts-red); }
.priority-yellow { background-color: var(--mts-yellow); color: var(--mts-yellow-text); }
```

From 07-RESEARCH.md Pattern 7 (focus trap implementation):
```jsx
const modalRef = useRef(null);
useEffect(() => {
  const modal = modalRef.current;
  if (!modal) return;
  const previousFocus = document.activeElement;
  const focusable = modal.querySelectorAll(
    'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
  );
  focusable[0]?.focus();
  const handleKeyDown = (e) => {
    if (e.key !== 'Tab') return;
    // Re-query on each keydown for dynamic content (tabs)
    const elements = modal.querySelectorAll(
      'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
    );
    const first = elements[0];
    const last = elements[elements.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault(); last.focus();
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault(); first.focus();
    }
  };
  modal.addEventListener('keydown', handleKeyDown);
  return () => { modal.removeEventListener('keydown', handleKeyDown); previousFocus?.focus(); };
}, []);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TriageDetailsModal.css with token-backed styles</name>
  <files>src/components/TriageDetailsModal.css</files>
  <read_first>src/components/TriageDetailsModal.jsx, src/components/Header.css, src/styles/tokens.css</read_first>
  <action>
Read the full TriageDetailsModal.jsx (507 lines, 71 inline styles). The file contains:
- Overlay backdrop
- Modal content container with tabs (Info, Chat, Sensores, Resultado)
- Close button (top-right X)
- Tab navigation row
- Patient info section with key-value pairs
- Chat history section with message bubbles
- Sensor data section
- Result section with priority badge
- PDF download button
- JS onMouseOver/onMouseOut hover handlers on buttons and tabs

Create `src/components/TriageDetailsModal.css` with BEM-prefixed classes (prefix: `.modal-`).

CSS structure — all values must use semantic tokens, zero raw hex:

```css
/* TriageDetailsModal.css — Token-backed classes */

/* Overlay */
.modal-overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
  padding: var(--spacing-xl);
}

/* Container */
.modal-content {
  background-color: var(--color-surface);
  border-radius: var(--radius-md);
  width: 100%;
  max-width: 900px;
  max-height: 85vh;
  overflow-y: auto;
  box-shadow: var(--shadow-md);
  position: relative;
}

/* Header area */
.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-lg) var(--spacing-xl);
  border-bottom: 1px solid var(--color-border);
}
.modal-title {
  margin: 0;
  color: var(--color-text-primary);
  font-size: var(--font-size-xl);
}

/* Close button */
.modal-close-btn {
  background: none;
  border: none;
  font-size: var(--font-size-xl);
  cursor: pointer;
  color: var(--color-text-secondary);
  padding: var(--spacing-sm);
  line-height: 1;
}
.modal-close-btn:hover {
  color: var(--color-text-primary);
}

/* Tab navigation */
.modal-tabs {
  display: flex;
  border-bottom: 2px solid var(--color-border);
  padding: 0 var(--spacing-xl);
}
.modal-tab {
  padding: var(--spacing-sm) var(--spacing-md);
  cursor: pointer;
  border: none;
  background: none;
  color: var(--color-text-secondary);  /* Replaced #adb5bd (2.07:1) — per D-20 */
  font-weight: 600;
  font-size: var(--font-size-sm);
  border-bottom: 2px solid transparent;
  margin-bottom: -2px;
  transition: color var(--transition-fast), border-color var(--transition-fast);
}
.modal-tab:hover {
  color: var(--color-text-primary);
}
.modal-tab--active {
  color: var(--color-primary);
  border-bottom-color: var(--color-primary);
}

/* Tab content area */
.modal-body {
  padding: var(--spacing-xl);
}

/* Info section — key-value pairs */
.modal-info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: var(--spacing-md);
}
.modal-info-item {}
.modal-info-label {
  font-size: var(--font-size-xs);
  color: var(--color-text-secondary);  /* Replaced #868e96 (3.32:1) — per D-20 */
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: var(--spacing-xs);
}
.modal-info-value {
  color: var(--color-text-primary);
  font-weight: 500;
}

/* Chat section */
.modal-chat-list {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
}
.modal-chat-bubble {
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--radius-md);
  max-width: 80%;
  font-size: var(--font-size-md);
}
.modal-chat-bubble--assistant {
  background-color: var(--color-surface-muted);
  color: var(--color-text-primary);
  align-self: flex-start;
}
.modal-chat-bubble--user {
  background-color: var(--color-primary);
  color: var(--color-primary-text);
  align-self: flex-end;
}

/* Sensor section */
.modal-sensor-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: var(--spacing-md);
}
.modal-sensor-card {
  background-color: var(--color-surface-subtle);
  padding: var(--spacing-md);
  border-radius: var(--radius-sm);
  border: 1px solid var(--color-border);
}
.modal-sensor-label {
  font-size: var(--font-size-xs);
  color: var(--color-text-secondary);
  font-weight: 600;
  text-transform: uppercase;
  margin-bottom: var(--spacing-xs);
}
.modal-sensor-value {
  font-size: var(--font-size-xl);
  font-weight: 600;
  color: var(--color-text-primary);
}

/* Result section */
.modal-result {
  text-align: center;
  padding: var(--spacing-xl);
}
.modal-result__priority {
  display: inline-block;
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--radius-sm);
  font-weight: 700;
  font-size: var(--font-size-xl);
  text-transform: uppercase;
}

/* PDF button */
.modal-pdf-btn {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-sm) var(--spacing-md);
  background-color: var(--color-surface);
  border: 1px solid var(--color-border-strong);
  border-radius: var(--radius-md);
  cursor: pointer;
  color: var(--color-primary);
  font-weight: 500;
  transition: all var(--transition-fast);
}
.modal-pdf-btn:hover {
  background-color: var(--color-primary);
  color: var(--color-primary-text);
  border-color: var(--color-primary);
}
```

Extend with additional classes as needed based on the full JSX read. Every visual property currently in an inline `style={{...}}` object MUST have a corresponding CSS class.
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && test -f src/components/TriageDetailsModal.css && wc -l src/components/TriageDetailsModal.css | awk '{if ($1 >= 80) exit 0; else exit 1}' && ! grep -qP '#[0-9a-fA-F]{3,8}' src/components/TriageDetailsModal.css</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TriageDetailsModal.css exists with 80+ lines
    - TriageDetailsModal.css contains zero raw hex color values (verify with grep for #xxx patterns — only rgba() for overlay is acceptable)
    - TriageDetailsModal.css contains `.modal-overlay` class
    - TriageDetailsModal.css contains `.modal-tab--active` class
    - TriageDetailsModal.css contains `.modal-close-btn` class
    - All hover states are CSS :hover rules (not JS handlers)
  </acceptance_criteria>
  <done>TriageDetailsModal.css created with full token-backed styling covering all 71 inline style equivalents.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TriageDetailsModal.jsx to use CSS classes + add ARIA + focus trap</name>
  <files>src/components/TriageDetailsModal.jsx</files>
  <read_first>src/components/TriageDetailsModal.jsx, src/components/TriageDetailsModal.css</read_first>
  <action>
**A. CSS migration (zero inline styles):**
- Add `import './TriageDetailsModal.css';` at top
- Add `import { useRef, useEffect } from 'react';` if not already imported
- Replace every `style={{...}}` with the corresponding `className="modal-*"` reference from TriageDetailsModal.css
- Remove ALL `onMouseOver`/`onMouseOut`/`onMouseEnter`/`onMouseLeave` JS hover handlers — CSS :hover handles these now
- Delete all inline style objects (the `style` props and any `const xxxStyle = {...}` objects)

**B. ARIA attributes (per D-15):**
- Add `ref={modalRef}` to the modal overlay div
- Add `role="dialog"` to the modal overlay div
- Add `aria-modal="true"` to the modal overlay div
- Add `aria-labelledby="modal-title"` to the modal overlay div
- Add `id="modal-title"` to the modal title element (the `<h2>` or heading that shows the protocol/patient name)
- Add `aria-label="Fechar janela"` to the close button (per UI-SPEC copywriting contract)
- Tab buttons: change from `<div onClick>` to `<button type="button">` if they are currently divs. Add `aria-selected={activeTab === tabName}` and `role="tab"`. Wrap the tab row in `role="tablist"`.

**C. Focus trap (native DOM implementation per D-15 and 07-RESEARCH.md Pattern 7):**
- Add a `useRef` for the modal container (`modalRef`)
- Add a `useEffect` that runs once when modal mounts:
  1. Save `document.activeElement` as `previousFocus`
  2. Query all focusable elements inside `modalRef.current`: `'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'`
  3. Focus the first focusable element
  4. Add a `keydown` event listener on the modal ref:
     - If key is `Tab`: re-query focusable elements (dynamic tabs content), get first and last. If Shift+Tab on first element, prevent default and focus last. If Tab on last element, prevent default and focus first.
     - If key is `Escape`: call `onClose()`
  5. Cleanup: remove keydown listener, restore `previousFocus.focus()`
- The `useEffect` cleanup must call `previousFocus?.focus()` to return focus to the trigger button when modal closes

**D. Contrast fixes applied during migration:**
- Tab inactive text: `#adb5bd` (2.07:1) → `.modal-tab` uses `--color-text-secondary` (7.63:1)
- Label uppercase text: `#868e96` (3.32:1) → `.modal-info-label` / `.modal-sensor-label` uses `--color-text-secondary` (7.63:1)
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && ! grep -q 'style={{' src/components/TriageDetailsModal.jsx && grep -q "import './TriageDetailsModal.css'" src/components/TriageDetailsModal.jsx && grep -q 'role="dialog"' src/components/TriageDetailsModal.jsx && grep -q 'aria-modal="true"' src/components/TriageDetailsModal.jsx && grep -q 'aria-labelledby' src/components/TriageDetailsModal.jsx && grep -q 'previousFocus' src/components/TriageDetailsModal.jsx && ! grep -q 'onMouseOver' src/components/TriageDetailsModal.jsx && ! grep -q 'onMouseOut' src/components/TriageDetailsModal.jsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TriageDetailsModal.jsx contains zero `style={{` occurrences
    - TriageDetailsModal.jsx contains `import './TriageDetailsModal.css'`
    - TriageDetailsModal.jsx contains `role="dialog"`
    - TriageDetailsModal.jsx contains `aria-modal="true"`
    - TriageDetailsModal.jsx contains `aria-labelledby="modal-title"`
    - TriageDetailsModal.jsx contains `id="modal-title"` on the heading element
    - TriageDetailsModal.jsx contains `aria-label="Fechar janela"` on close button
    - TriageDetailsModal.jsx contains focus trap logic (previousFocus variable, keydown handler with Tab key check)
    - TriageDetailsModal.jsx contains zero `onMouseOver` or `onMouseOut` handlers
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>TriageDetailsModal fully migrated: zero inline styles, ARIA dialog attributes, focus trap with restore-on-close, all contrast failures fixed, JS hover handlers eliminated.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -c 'style={{' src/components/TriageDetailsModal.jsx` returns 0
- `grep 'role="dialog"' src/components/TriageDetailsModal.jsx` matches
- `grep 'aria-modal="true"' src/components/TriageDetailsModal.jsx` matches
- `grep '#adb5bd\|#868e96' src/components/TriageDetailsModal.jsx` returns empty
</verification>

<success_criteria>
- TriageDetailsModal.jsx has zero inline styles
- Modal has role="dialog", aria-modal="true", aria-labelledby (A11Y-02)
- Focus trap prevents Tab from escaping modal (A11Y-02)
- Focus returns to trigger element on close (A11Y-02)
- Escape key closes modal
- All contrast-failing colors replaced with AA-compliant tokens (A11Y-01)
- JS hover handlers eliminated in favor of CSS :hover
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-component-migration-accessibility/07-03-SUMMARY.md`
</output>
