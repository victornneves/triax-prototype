---
phase: 06-ui-primitives-toast
plan: "03"
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/components/HistoryPage.jsx
  - src/components/TriageDetailsModal.jsx
  - src/components/ProtocolTriage.jsx
  - src/index.css
autonomous: true
requirements: [INTR-01, INTR-04]

must_haves:
  truths:
    - "Zero alert() calls remain in the codebase"
    - "All 5 former alert sites now show toast error notifications with identical PT-BR messages"
    - "Chat bubbles in ProtocolTriage animate in with fade+translateY under 300ms"
    - "The .animate-fade-in utility class exists in index.css backed by existing fadeIn keyframe"
  artifacts:
    - path: "src/components/HistoryPage.jsx"
      provides: "Toast-based error feedback for PDF and session detail failures"
      contains: "useToast"
    - path: "src/components/TriageDetailsModal.jsx"
      provides: "Toast-based error feedback for PDF download failure"
      contains: "useToast"
    - path: "src/components/ProtocolTriage.jsx"
      provides: "Toast-based error feedback + animated chat bubbles"
      contains: "animate-fade-in"
    - path: "src/index.css"
      provides: ".animate-fade-in utility class"
      contains: ".animate-fade-in"
  key_links:
    - from: "src/components/HistoryPage.jsx"
      to: "src/components/ui/ToastProvider.jsx"
      via: "useToast() hook import"
      pattern: "import.*useToast.*from.*ui/ToastProvider"
    - from: "src/components/ProtocolTriage.jsx"
      to: "src/index.css"
      via: "className animate-fade-in"
      pattern: "animate-fade-in"
---

<objective>
Replace all 5 alert() calls with toast notifications and add chat bubble fade-in animation.

Purpose: INTR-01 requires all alert() sites replaced with accessible toasts. INTR-04 requires animated chat bubbles under 300ms. This plan performs the actual integration work using the toast system built in Plan 02.
Output: Updated `HistoryPage.jsx`, `TriageDetailsModal.jsx`, `ProtocolTriage.jsx`, updated `src/index.css`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-primitives-toast/06-CONTEXT.md
@.planning/phases/06-ui-primitives-toast/06-RESEARCH.md
@.planning/phases/06-ui-primitives-toast/06-UI-SPEC.md

<!-- Plan 02 SUMMARY will exist by Wave 2 -->
@.planning/phases/06-ui-primitives-toast/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: ToastProvider API -->
From src/components/ui/ToastProvider.jsx:
```jsx
export const useToast = () => useContext(ToastContext);
// Returns: { error: (msg) => void, success: (msg) => void, info: (msg) => void }
```

<!-- Usage pattern in any component: -->
```jsx
import { useToast } from '../ui/ToastProvider';
// Inside component:
const toast = useToast();
// Replace: alert("Erro message")
// With:    toast.error("Erro message")
```

<!-- Existing fadeIn keyframe in index.css (line 69-72): -->
```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(5px); }
  to { opacity: 1; transform: translateY(0); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all alert() calls with toast notifications</name>
  <files>src/components/HistoryPage.jsx, src/components/TriageDetailsModal.jsx, src/components/ProtocolTriage.jsx</files>
  <read_first>
    - src/components/HistoryPage.jsx
    - src/components/TriageDetailsModal.jsx
    - src/components/ProtocolTriage.jsx
    - .planning/phases/06-ui-primitives-toast/06-UI-SPEC.md (Alert Call Site Replacement Map section)
  </read_first>
  <action>
Replace all 5 `alert()` calls 1:1 with `toast.error()` using identical PT-BR messages. Per D-09, same trigger conditions, same error messages, just rendered as toast instead of blocking dialog.

**For each file, make TWO changes:**

**A) Add import and hook call:**

At the top of the file, add:
```jsx
import { useToast } from './ui/ToastProvider';
```

Inside the component function body (before any hooks), add:
```jsx
const toast = useToast();
```

Note: `TriageDetailsModal` receives props, not hooks — but it is rendered inside a component tree with `ToastProvider`, so `useToast()` works. Verify that `TriageDetailsModal` is a function component (it is) so hooks are valid.

**B) Replace each alert() call:**

**File: `src/components/HistoryPage.jsx`**
- Line ~35: Replace `alert("Erro ao gerar PDF. Tente novamente.")` with `toast.error("Erro ao gerar PDF. Tente novamente.")`
- Line ~81: Replace `alert("Erro ao carregar detalhes da sessao.")` with `toast.error("Erro ao carregar detalhes da sessao.")`

**File: `src/components/TriageDetailsModal.jsx`**
- Line ~129: Replace `alert("Erro ao gerar PDF. Tente novamente.")` with `toast.error("Erro ao gerar PDF. Tente novamente.")`

**File: `src/components/ProtocolTriage.jsx`**
- Line ~401: Replace `alert("Erro ao iniciar sessao. Tente novamente.")` with `toast.error("Erro ao iniciar sessao. Tente novamente.")`
- Line ~846: Replace `alert("Erro ao gerar PDF. Tente novamente.")` with `toast.error("Erro ao gerar PDF. Tente novamente.")`

IMPORTANT: Do NOT change anything else about the error handling logic. The `try/catch` blocks, the `finally` blocks, the `setLoading` calls — all remain exactly as-is. Only the `alert(...)` call itself is replaced with `toast.error(...)`.

IMPORTANT: Read each file first to find the exact current line numbers — the numbers above are from the RESEARCH phase and may have shifted.
  </action>
  <verify>
    <automated>grep -rn "alert(" /home/victor/Git/triax-prototype/src/components/HistoryPage.jsx /home/victor/Git/triax-prototype/src/components/TriageDetailsModal.jsx /home/victor/Git/triax-prototype/src/components/ProtocolTriage.jsx 2>/dev/null; echo "---"; grep -c "useToast" /home/victor/Git/triax-prototype/src/components/HistoryPage.jsx /home/victor/Git/triax-prototype/src/components/TriageDetailsModal.jsx /home/victor/Git/triax-prototype/src/components/ProtocolTriage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Zero `alert(` calls in HistoryPage.jsx, TriageDetailsModal.jsx, ProtocolTriage.jsx (grep returns empty)
    - HistoryPage.jsx contains `import { useToast } from './ui/ToastProvider'`
    - HistoryPage.jsx contains `const toast = useToast()`
    - HistoryPage.jsx contains `toast.error("Erro ao gerar PDF. Tente novamente.")`
    - HistoryPage.jsx contains `toast.error("Erro ao carregar detalhes da sessao.")`  (note: check exact accent — match existing string byte-for-byte)
    - TriageDetailsModal.jsx contains `import { useToast } from './ui/ToastProvider'`
    - TriageDetailsModal.jsx contains `toast.error("Erro ao gerar PDF. Tente novamente.")`
    - ProtocolTriage.jsx contains `import { useToast } from './ui/ToastProvider'`
    - ProtocolTriage.jsx contains `toast.error("Erro ao iniciar sessao. Tente novamente.")` (check exact accent)
    - ProtocolTriage.jsx contains `toast.error("Erro ao gerar PDF. Tente novamente.")`
    - No other changes to error handling logic (try/catch/finally blocks unchanged)
    - `npm run dev` starts without errors
  </acceptance_criteria>
  <done>All 5 alert() calls replaced with toast.error() using identical PT-BR error messages; useToast hook imported and called in all 3 files</done>
</task>

<task type="auto">
  <name>Task 2: Add .animate-fade-in utility class and apply to chat bubbles</name>
  <files>src/index.css, src/components/ProtocolTriage.jsx</files>
  <read_first>
    - src/index.css
    - src/components/ProtocolTriage.jsx
    - .planning/phases/06-ui-primitives-toast/06-RESEARCH.md (Pattern 6 section)
  </read_first>
  <action>
Per D-16 (extend existing fadeIn keyframe) and INTR-04 (animations under 300ms).

**1. Add utility class to `src/index.css`:**

After the existing `@keyframes fadeIn` block (currently around line 72), add:

```css
.animate-fade-in {
  animation: fadeIn 200ms ease-out forwards;
  will-change: opacity, transform;
}
```

The `will-change` promotes the element to its own compositor layer, preventing layout jitter with the auto-scroll `scrollIntoView` in ProtocolTriage (per Pitfall 5 in RESEARCH.md).

**2. Add className to chat bubble wrapper in `src/components/ProtocolTriage.jsx`:**

Find the message rendering map — look for `<div key={idx}` inside the messages `.map()` that renders chat bubbles. The wrapper div has inline styles for `display: 'flex'`, `justifyContent`, and `marginBottom`.

Add `className="animate-fade-in"` to that div element. Do NOT remove any existing inline styles — just add the className attribute.

Before:
```jsx
<div key={idx} style={{
  display: 'flex',
  justifyContent: msg.role === 'user' ? 'flex-end' : 'flex-start',
  marginBottom: '1rem'
}}>
```

After:
```jsx
<div key={idx} className="animate-fade-in" style={{
  display: 'flex',
  justifyContent: msg.role === 'user' ? 'flex-end' : 'flex-start',
  marginBottom: '1rem'
}}>
```

IMPORTANT: Read ProtocolTriage.jsx first to find the exact location. Look for the `.map(` call that renders messages/chat bubbles. The variable may be named `messages`, `conversationHistory`, or similar. The `key={idx}` pattern identifies the wrapper div.

This is a single attribute addition — no structural changes to the component.
  </action>
  <verify>
    <automated>grep "animate-fade-in" /home/victor/Git/triax-prototype/src/index.css /home/victor/Git/triax-prototype/src/components/ProtocolTriage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `.animate-fade-in` class definition
    - src/index.css `.animate-fade-in` rule contains `animation: fadeIn 200ms ease-out forwards`
    - src/index.css `.animate-fade-in` rule contains `will-change: opacity, transform`
    - src/components/ProtocolTriage.jsx contains `className="animate-fade-in"` on the chat bubble wrapper div
    - The chat bubble div still has its existing inline `style={{...}}` (inline styles NOT removed)
    - `npm run dev` starts without errors
    - No other structural changes to ProtocolTriage.jsx besides the className addition and the alert->toast changes from Task 1
  </acceptance_criteria>
  <done>Chat bubble messages animate in with 200ms fade+translateY using the existing fadeIn keyframe; .animate-fade-in utility class exists in index.css for reuse</done>
</task>

</tasks>

<verification>
- `grep -r "alert(" src/components/` returns zero matches (all alert() calls removed)
- `grep -r "useToast" src/components/` returns matches in HistoryPage.jsx, TriageDetailsModal.jsx, ProtocolTriage.jsx
- `grep "animate-fade-in" src/index.css src/components/ProtocolTriage.jsx` returns matches in both files
- `npm run dev` starts without errors
- Triggering an error in the app (e.g., network failure on PDF download) shows a toast notification instead of a browser alert dialog
</verification>

<success_criteria>
- All 5 alert() call sites replaced with toast.error() — zero alert() calls remain
- Chat bubbles animate with 200ms fadeIn effect on render
- All error messages preserved byte-for-byte in PT-BR
- No changes to error handling logic beyond the alert->toast swap
- App runs without errors on `npm run dev`
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-primitives-toast/06-03-SUMMARY.md`
</output>
