---
phase: 04-fragility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Profile.jsx
  - src/useTranscribe.js
autonomous: true
requirements: [FRAG-02, FRAG-03]
must_haves:
  truths:
    - "Profile.jsx contains no formatDateFromKey function — the dead code is removed"
    - "Session dates in Profile.jsx display correctly via item.created_at (already in production)"
    - "useTranscribe.js contains no call to escape() — the deprecated function is removed"
    - "Transcription text from AWS Transcribe SDK displays correctly without the encoding workaround"
  artifacts:
    - path: "src/pages/Profile.jsx"
      provides: "Clean date handling via created_at API field"
      contains: "created_at"
    - path: "src/useTranscribe.js"
      provides: "Clean transcript text handling without deprecated escape()"
  key_links:
    - from: "Profile.jsx session list rendering"
      to: "item.created_at from /history API"
      via: "toLocaleString at line 113 (already working)"
      pattern: "item\\.created_at"
---

<objective>
Remove dead code (formatDateFromKey in Profile.jsx) and deprecated API usage (escape() in useTranscribe.js).

Purpose: formatDateFromKey parses S3 key filenames for dates but is never called — the JSX already uses item.created_at. The escape() call in useTranscribe.js is a deprecated workaround that does nothing useful since the AWS SDK returns properly decoded strings.
Output: Cleaner Profile.jsx and useTranscribe.js with no deprecated or dead code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fragility/04-CONTEXT.md
@.planning/phases/04-fragility/04-RESEARCH.md

<interfaces>
<!-- Profile.jsx dead code to remove (lines 43-67): -->
const formatDateFromKey = (key) => {
    if (!key) return '-';
    try {
        // key example: default/UUID/YEAR/MONTH/DAY/session-TIMESTAMP-RANDOM.json
        const filename = key.split('/').pop();
        const parts = filename.split('-');
        if (parts.length < 2) return '-';
        const timestamp = parseInt(parts[1], 10);
        if (isNaN(timestamp)) return '-';
        const date = new Date(timestamp);
        return date.toLocaleString('pt-BR', {
            timeZone: 'America/Sao_Paulo',
            day: '2-digit', month: '2-digit', year: 'numeric',
            hour: '2-digit', minute: '2-digit'
        });
    } catch (e) {
        console.error("Error parsing date from key", key, e);
        return '-';
    }
};

<!-- Profile.jsx line 113 already uses created_at (no change needed): -->
{item.created_at ? new Date(item.created_at).toLocaleString('pt-BR', { timeZone: 'America/Sao_Paulo' }) : '-'}

<!-- useTranscribe.js deprecated code to remove (lines 137-139): -->
try {
    text = decodeURIComponent(escape(text));
} catch { }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead formatDateFromKey function from Profile.jsx</name>
  <files>src/pages/Profile.jsx</files>
  <read_first>
    - src/pages/Profile.jsx (full file — confirm formatDateFromKey is defined but never called in JSX or other functions)
    - .planning/phases/04-fragility/04-RESEARCH.md (FRAG-02 finding section)
  </read_first>
  <action>
    Delete the entire `formatDateFromKey` function from Profile.jsx (lines 43-67, the function definition from `const formatDateFromKey = (key) => {` through its closing `};`).

    Before deleting, verify that:
    1. `formatDateFromKey` is NOT called anywhere in the file's JSX or other functions (search the file for "formatDateFromKey" — it should only appear in the function definition)
    2. The rendered table at line 113 already uses `item.created_at ? new Date(item.created_at).toLocaleString('pt-BR', { timeZone: 'America/Sao_Paulo' }) : '-'`
    3. The sort at lines 26-30 uses `a.created_at` / `b.created_at`

    If formatDateFromKey IS called somewhere (contradicting research), do NOT delete it — instead replace its body to use created_at. But research confirms it is dead code.

    Do NOT modify any other part of Profile.jsx. Do NOT change the existing created_at usage at line 113.
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && grep -n "formatDateFromKey" src/pages/Profile.jsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "formatDateFromKey" src/pages/Profile.jsx returns 0 (function completely removed)
    - grep "created_at" src/pages/Profile.jsx returns matches (existing created_at usage preserved)
    - grep "toLocaleString.*pt-BR" src/pages/Profile.jsx returns a match (date formatting preserved)
    - npm run build succeeds with no errors
  </acceptance_criteria>
  <done>formatDateFromKey dead code is removed from Profile.jsx. Session dates continue to display correctly via the existing item.created_at path.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated escape() workaround from useTranscribe.js</name>
  <files>src/useTranscribe.js</files>
  <read_first>
    - src/useTranscribe.js (full file — see the escape() block around lines 137-139)
    - .planning/phases/04-fragility/04-RESEARCH.md (FRAG-03 finding section and Pattern 5)
  </read_first>
  <action>
    Delete the entire try/catch block that wraps `decodeURIComponent(escape(text))` from useTranscribe.js (around lines 137-139):

    ```javascript
    // DELETE THIS BLOCK:
    try {
        text = decodeURIComponent(escape(text));
    } catch { }
    ```

    The `let text = alternatives[0].Transcript || "";` line BEFORE the block stays. The `if (result.IsPartial)` block AFTER stays. Only the try/catch is removed.

    Reason: AWS Transcribe Streaming SDK returns `Transcript` as a JavaScript string (natively UTF-16). There are no Latin-1 bytes to convert. The `escape()` function is deprecated since ES3 and the workaround does nothing useful here.

    Do NOT replace with TextDecoder — deletion is the correct fix per research finding. Do NOT add any replacement encoding logic.
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && grep -n "escape(" src/useTranscribe.js; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "escape(" src/useTranscribe.js returns 0 (deprecated call removed)
    - grep -c "decodeURIComponent" src/useTranscribe.js returns 0 (entire workaround removed)
    - grep "alternatives\[0\].Transcript" src/useTranscribe.js returns a match (text assignment preserved)
    - grep "IsPartial" src/useTranscribe.js returns a match (downstream logic preserved)
    - npm run build succeeds with no errors
  </acceptance_criteria>
  <done>The deprecated escape() workaround is removed from useTranscribe.js. Transcript text flows directly from the SDK without unnecessary re-encoding.</done>
</task>

</tasks>

<verification>
1. `grep -n "formatDateFromKey" src/pages/Profile.jsx` — returns nothing (dead code removed)
2. `grep -n "escape(" src/useTranscribe.js` — returns nothing (deprecated call removed)
3. `npm run build` — no build errors
</verification>

<success_criteria>
- formatDateFromKey function is completely absent from Profile.jsx
- Session dates still display via item.created_at (no regression)
- escape() call is completely absent from useTranscribe.js
- Transcript text assignment and IsPartial check remain intact
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-fragility/04-02-SUMMARY.md`
</output>
