---
phase: 10-sensor-panel-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SensorPanel.jsx
  - src/components/SensorPanel.css
  - src/components/ProtocolTriage.jsx
  - src/components/ProtocolTriage.css
autonomous: true
requirements: [REFAC-01]

must_haves:
  truths:
    - "Sensor panel renders identically in desktop sidebar and mobile slide-up from a single SensorPanel component"
    - "No duplicated sensor panel rendering logic remains in ProtocolTriage.jsx"
    - "Desktop sidebar and mobile slide-up vital sign behavior (inputs, expand/collapse, submit) is unchanged"
    - "SensorPanel accepts an optional getFieldStatus callback for Phase 12 forward-compatibility"
    - "Mobile BP inputs have identical validation attributes (type, maxLength, min, max) as desktop via unification"
  artifacts:
    - path: "src/components/SensorPanel.jsx"
      provides: "Shared sensor panel component with PainInput, GCSInput, SensorLabel sub-components and SENSOR_CONFIG"
      exports: ["default SensorPanel"]
      min_lines: 120
    - path: "src/components/SensorPanel.css"
      provides: "Co-located CSS for sensor panel, extracted from ProtocolTriage.css"
      contains: ".triage-sensors"
    - path: "src/components/ProtocolTriage.jsx"
      provides: "Triage page using SensorPanel instead of inline sensor rendering"
      contains: "import SensorPanel"
    - path: "src/components/ProtocolTriage.css"
      provides: "Triage CSS without sensor-specific styles (moved to SensorPanel.css)"
  key_links:
    - from: "src/components/SensorPanel.jsx"
      to: "src/components/ui/Tooltip.jsx"
      via: "import in SensorLabel"
      pattern: "import.*Tooltip.*from.*ui/Tooltip"
    - from: "src/components/ProtocolTriage.jsx"
      to: "src/components/SensorPanel.jsx"
      via: "import and render in desktop sidebar and mobile aside"
      pattern: "import SensorPanel from.*SensorPanel"
    - from: "src/components/SensorPanel.jsx"
      to: "src/components/SensorPanel.css"
      via: "CSS import"
      pattern: "import.*SensorPanel\\.css"
---

<objective>
Extract the duplicated sensor panel rendering from ProtocolTriage.jsx into a single shared SensorPanel component, eliminating ~170 lines of duplication between desktop (lines 1085-1192) and mobile (lines 1197-1260) sensor panel blocks.

Purpose: REFAC-01 — unify sensor panel rendering so Phase 12 (Vital Signs UX) can add abnormal/critical indicators to one component instead of two. Follows the PatientForm extraction pattern from Phase 9.

Output: SensorPanel.jsx + SensorPanel.css (new), ProtocolTriage.jsx + ProtocolTriage.css (simplified)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/ProtocolTriage.jsx
@src/components/ProtocolTriage.css
@src/components/PatientForm.jsx (extraction pattern reference)
@src/components/PatientForm.css (co-located CSS pattern reference)
@src/components/ui/Tooltip.jsx (used by SensorLabel)
@src/styles/tokens.css (design tokens)
@src/index.css (sensor-missing-pulse animation — stays here, shared)

<interfaces>
<!-- Key types and contracts the executor needs. -->

SensorPanel props interface (new — designed per D-01 through D-07):
```jsx
// Props received from ProtocolTriage:
// sensorInputs     — object: { [sensorKey]: string } — current input values
// missingSensors   — string[]: keys flagged by API as missing (red pulse highlight)
// onSensorChange   — (e: Event) => void — updates sensorInputs in parent
// onSubmit         — () => void — triggers handleSendSensors in parent
// loading          — boolean — disables submit button, shows "Processando..."
// getFieldStatus   — (key: string, value: string) => string|null — optional, Phase 12 hook (D-06)
```

Sub-components moving to SensorPanel.jsx:
```jsx
// SENSOR_CONFIG — object with 8 vital sign entries (lines 12-76)
// PainInput({ value, onChange }) — pain scale slider 0-10 (lines 78-97)
// GCSInput({ value, onChange }) — Glasgow dropdown 3-15 (lines 99-129)
// SensorLabel({ config }) — label + tooltip + hint (lines 131-142)
```

CSS classes moving to SensorPanel.css (from ProtocolTriage.css):
```
.triage-sensors              (line 314)
.triage-sensors__header      (line 326)
.triage-sensors__title       (line 335)
.triage-sensors__fill-btn    (line 341)
.triage-sensors__list        (line 355)
.triage-sensors__item        (line 363)
.triage-sensors__item--missing (line 375)
.triage-sensors__input       (line 380)
.triage-sensors__bp-group    (line 396)
.triage-sensors__bp-input    (line 402)
.triage-sensors__bp-separator (line 419)
.triage-sensors__submit-btn  (line 424)
.sensor-label-group          (line 450 — mapped from .triage-sensors__label)
.triage-sensors__gcs-select  (line 468)
.triage-sensors__pain-group  (line 480)
.triage-sensors__pain-range  (line 488)
.triage-sensors__pain-value  (line 494)
.triage-sensors__label       (line 799)
.triage-sensors__label-text  (line 805)
.triage-sensors__hint        (line 814)
```

CSS classes that STAY in ProtocolTriage.css (mobile chrome, not sensor-specific):
```
.triage-sensors-column       (desktop sidebar container)
.triage-sensors-aside        (mobile slide-up container)
.triage-sensors-aside--open  (mobile open state)
.triage-sensors__summary     (mobile vitals summary strip)
.triage-sensors__toggle      (mobile toggle pill button)
.triage-sensors__backdrop    (mobile overlay)
.triage-session-info*        (session info card — outside SensorPanel per D-02)
.triage-cancel-btn           (cancel button — outside SensorPanel per D-02)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SensorPanel component and co-located CSS</name>
  <files>src/components/SensorPanel.jsx, src/components/SensorPanel.css</files>
  <read_first>
    - src/components/ProtocolTriage.jsx (lines 1-143 for SENSOR_CONFIG, PainInput, GCSInput, SensorLabel; lines 1085-1192 for desktop sensor block; lines 1197-1260 for mobile sensor block — these are the source of truth for what to extract)
    - src/components/ProtocolTriage.css (lines 314-500 for sensor card styles; lines 799-817 for sensor label styles)
    - src/components/PatientForm.jsx (lines 1-5 for import pattern; line 23 for prop interface pattern)
    - src/components/PatientForm.css (lines 1-10 for co-located CSS import pattern)
    - src/components/ui/Tooltip.jsx (confirm import path for SensorLabel)
    - src/styles/tokens.css (confirm token var names used in sensor CSS)
  </read_first>
  <action>
Create `src/components/SensorPanel.jsx` containing:

1. **Move SENSOR_CONFIG** (currently ProtocolTriage.jsx lines 12-76) to the top of SensorPanel.jsx. This is an object with 8 entries: `blood_glucose`, `blood_pressure`, `gcs`, `heart_rate`, `oxygen_saturation`, `pain_scale`, `respiratory_rate`, `temperature`. Each entry has `label`, `full_label`, `hint`, `desc`, `range`, and optionally `composite`, `type`, `min`, `max`, `emoji`.

2. **Move PainInput** (currently lines 78-97): functional component accepting `{ value, onChange }`. Renders a range slider 0-10 with dynamic `accentColor` based on pain severity. Uses classes `triage-sensors__pain-group`, `triage-sensors__pain-range`, `triage-sensors__pain-value`.

3. **Move GCSInput** (currently lines 99-129): functional component accepting `{ value, onChange }`. Renders a `<select>` with options 15 down to 3. Dynamic inline border color is a clinical severity indicator (red <= 8, yellow <= 12, green otherwise). Uses class `triage-sensors__gcs-select`.

4. **Move SensorLabel** (currently lines 131-142): functional component accepting `{ config }`. Renders label text + `<Tooltip>` + hint. Import `Tooltip` from `./ui/Tooltip`. Uses classes `triage-sensors__label`, `triage-sensors__label-text`, `triage-sensors__hint`.

5. **Create the SensorPanel default export** as a functional component with this exact prop interface:
```jsx
const SensorPanel = ({ sensorInputs, missingSensors, onSensorChange, onSubmit, loading, getFieldStatus }) => {
```

The component renders:
- An `<h4>` with class `triage-sensors__title` containing "Sinais Vitais"
- A sensor list div with class `triage-sensors__list` that maps over `Object.keys(SENSOR_CONFIG)` and for each key:
  - Determines `isMissing` via `missingSensors.includes(key)`
  - Determines `fieldStatus` via `getFieldStatus ? getFieldStatus(key, sensorInputs[key]) : null` (per D-06)
  - Selects the correct input component: `PainInput` for `pain_scale`, `GCSInput` for `gcs`, composite BP group for `blood_pressure`, or standard `<input type="number">` otherwise
  - For the BP composite group, render BOTH inputs with `type="number"`, `maxLength={3}`, `min={0}`, `max={300}` attributes (per D-08 — unifies desktop attributes onto both)
  - Wraps in a div with class `triage-sensors__item` and conditional `triage-sensors__item--missing sensor-missing-pulse` when `isMissing`
  - If `fieldStatus` is truthy, adds a `data-field-status={fieldStatus}` attribute on the item wrapper div (per D-07)
  - Renders `<SensorLabel config={conf} />` and the input component
- A submit button with class `triage-sensors__submit-btn`, `onClick={onSubmit}`, `disabled={loading}`, text "Processando..." when loading or "Atualizar Sinais Vitais" otherwise

Wrap everything in a `<div className="triage-sensors">`.

Import `'./SensorPanel.css'` at the top (per co-located CSS pattern).

6. **Create `src/components/SensorPanel.css`** by extracting these class blocks from ProtocolTriage.css:
- `.triage-sensors` (line 314-324)
- `.triage-sensors__header` (line 326-332) — keep even if unused, for completeness
- `.triage-sensors__title` (line 335-338)
- `.triage-sensors__fill-btn` and `:hover` (line 341-353)
- `.triage-sensors__list` (line 355-361)
- `.triage-sensors__item` and `--missing` (line 363-378)
- `.triage-sensors__input` and `:focus` (line 380-394)
- `.triage-sensors__bp-group` (line 396-400)
- `.triage-sensors__bp-input` and `:focus` (line 402-417)
- `.triage-sensors__bp-separator` (line 419-422)
- `.triage-sensors__submit-btn`, `:hover`, `:disabled` (line 424-447)
- `.triage-sensors__gcs-select` (line 468-477)
- `.triage-sensors__pain-group` (line 480-486)
- `.triage-sensors__pain-range` (line 488-492)
- `.triage-sensors__pain-value` (line 494-499)
- `.triage-sensors__label` (line 799-803)
- `.triage-sensors__label-text` (line 805-812)
- `.triage-sensors__hint` (line 814-817)

Do NOT move to SensorPanel.css:
- `.sensor-label-group*` classes (lines 450-465) — if present, check if actually used; the actual label classes are `.triage-sensors__label*`
- `.triage-sensors__summary` (mobile strip — stays in ProtocolTriage.css)
- `.triage-sensors__toggle` (mobile toggle — stays in ProtocolTriage.css)
- `.triage-sensors__backdrop` (mobile backdrop — stays in ProtocolTriage.css)
- `.triage-sensors-column` (desktop container — stays in ProtocolTriage.css)
- `.triage-sensors-aside*` (mobile aside container — stays in ProtocolTriage.css)
- `.triage-session-info*` (session info card — stays per D-02)
- `.triage-cancel-btn` (cancel button — stays per D-02)
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && node -e "import('./src/components/SensorPanel.jsx').then(m => { console.log('default export:', typeof m.default); if (typeof m.default !== 'function') process.exit(1); })" 2>&1 || echo "FAIL: SensorPanel.jsx does not export a valid component" ; test -f src/components/SensorPanel.css && echo "PASS: SensorPanel.css exists" || echo "FAIL: SensorPanel.css missing"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SensorPanel.jsx exists and contains `export default SensorPanel`
    - SensorPanel.jsx contains `const SENSOR_CONFIG = {` with all 8 keys: `blood_glucose`, `blood_pressure`, `gcs`, `heart_rate`, `oxygen_saturation`, `pain_scale`, `respiratory_rate`, `temperature`
    - SensorPanel.jsx contains `const PainInput = (`
    - SensorPanel.jsx contains `const GCSInput = (`
    - SensorPanel.jsx contains `const SensorLabel = (`
    - SensorPanel.jsx contains `import { Tooltip } from './ui/Tooltip'`
    - SensorPanel.jsx contains `import './SensorPanel.css'`
    - SensorPanel.jsx prop destructuring contains `getFieldStatus` parameter
    - SensorPanel.jsx contains `data-field-status` attribute assignment
    - SensorPanel.jsx BP inputs contain `type="number"` and `maxLength={3}` and `min={0}` and `max={300}` (D-08 unification)
    - src/components/SensorPanel.css exists and contains `.triage-sensors {`
    - SensorPanel.css contains `.triage-sensors__submit-btn {`
    - SensorPanel.css contains `.triage-sensors__label {`
    - SensorPanel.css contains `.triage-sensors__gcs-select {`
    - SensorPanel.css contains `.triage-sensors__pain-group {`
  </acceptance_criteria>
  <done>SensorPanel.jsx exports a functional component with SENSOR_CONFIG, PainInput, GCSInput, SensorLabel. SensorPanel.css contains all sensor-specific styles. The getFieldStatus callback prop is wired with data-field-status attribute passthrough per D-06/D-07.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SensorPanel into ProtocolTriage and remove duplicated code</name>
  <files>src/components/ProtocolTriage.jsx, src/components/ProtocolTriage.css</files>
  <read_first>
    - src/components/SensorPanel.jsx (just created in Task 1 — read to confirm exact export name and prop interface)
    - src/components/ProtocolTriage.jsx (full file — understand what to remove and what to keep)
    - src/components/ProtocolTriage.css (full file — understand which CSS blocks were moved vs which stay)
    - src/components/PatientForm.jsx (lines 1-5 — confirm import pattern: `import PatientForm from './PatientForm'`)
  </read_first>
  <action>
Modify `src/components/ProtocolTriage.jsx`:

1. **Add import** near the top (after the PatientForm import on line 6):
```jsx
import SensorPanel from './SensorPanel';
```

2. **Remove moved code** — delete these blocks entirely:
- Lines 12-76: `const SENSOR_CONFIG = { ... };` (moved to SensorPanel.jsx)
- Lines 78-97: `const PainInput = ({ value, onChange }) => { ... };` (moved to SensorPanel.jsx)
- Lines 99-129: `const GCSInput = ({ value, onChange }) => { ... };` (moved to SensorPanel.jsx)
- Lines 131-142: `const SensorLabel = ({ config }) => { ... };` (moved to SensorPanel.jsx)

3. **Replace desktop sensor block** (currently lines 1107-1192 — the `{/* Sensors */}` section inside `.triage-sensors-column`) with:
```jsx
<SensorPanel
    sensorInputs={sensorInputs}
    missingSensors={missingSensors}
    onSensorChange={handleSensorChange}
    onSubmit={handleSendSensors}
    loading={loading}
/>
```
Keep the session info card (`triage-session-info` div with patient name, protocol, cancel button — lines 1088-1105) exactly as-is, ABOVE the SensorPanel. Only the `<div className="triage-sensors">` block (lines 1108-1192) gets replaced.

4. **Replace mobile sensor block** (currently lines 1221-1259 — the `{/* Sensors */}` section inside the `<aside>`) with the same:
```jsx
<SensorPanel
    sensorInputs={sensorInputs}
    missingSensors={missingSensors}
    onSensorChange={handleSensorChange}
    onSubmit={handleSendSensors}
    loading={loading}
/>
```
Keep the mobile session info card (lines 1203-1219) exactly as-is, ABOVE the SensorPanel. Only the `<div className="triage-sensors">` block (lines 1222-1259) gets replaced.

5. **Keep all of these in ProtocolTriage.jsx** (do NOT remove):
- `sensorInputs` state (line 164)
- `sensorPanelOpen` state (line 166)
- `missingSensors` state (line 770)
- `handleSensorChange` function (lines 772-774)
- `handleSendSensors` function (lines 776-792)
- `submitSensorData` function (lines 529-563)
- Desktop `.triage-sensors-column` container div (line 1085)
- Mobile `<aside>` container (lines 1197-1260 container, keeping session info + replacing sensor block)
- Mobile vitals summary strip (lines 1262-1265)
- Mobile toggle button (lines 1267-1275)
- Mobile backdrop (lines 1277-1284)
- The `import { Tooltip } from './ui/Tooltip'` can be REMOVED from ProtocolTriage.jsx IF Tooltip is no longer directly used there — check if any other JSX in the file uses `<Tooltip>`. If only SensorLabel used it, remove the import.

6. **Modify `src/components/ProtocolTriage.css`** — remove the CSS blocks that were moved to SensorPanel.css:
- Remove `.triage-sensors {` block (lines 314-324)
- Remove `.triage-sensors__header` block (lines 326-332)
- Remove `.triage-sensors__title` block (lines 335-338)
- Remove `.triage-sensors__fill-btn` and `:hover` blocks (lines 341-353)
- Remove `.triage-sensors__list` block (lines 355-361)
- Remove `.triage-sensors__item` and `--missing` blocks (lines 363-378)
- Remove `.triage-sensors__input` and `:focus` blocks (lines 380-394)
- Remove `.triage-sensors__bp-group` block (lines 396-400)
- Remove `.triage-sensors__bp-input` and `:focus` blocks (lines 402-417)
- Remove `.triage-sensors__bp-separator` block (lines 419-422)
- Remove `.triage-sensors__submit-btn`, `:hover`, `:disabled` blocks (lines 424-447)
- Remove `.sensor-label-group*` blocks (lines 450-465) if they exist
- Remove `.triage-sensors__gcs-select` block (lines 468-477)
- Remove `.triage-sensors__pain-group` block (lines 480-486)
- Remove `.triage-sensors__pain-range` block (lines 488-492)
- Remove `.triage-sensors__pain-value` block (lines 494-499)
- Remove `.triage-sensors__label` block (lines 799-803)
- Remove `.triage-sensors__label-text` block (lines 805-812)
- Remove `.triage-sensors__hint` block (lines 814-817)

Keep in ProtocolTriage.css:
- `.triage-sensors__summary` (line 501-510) — mobile vitals strip
- `.triage-sensors__toggle` (line 513-515) — mobile toggle
- `.triage-sensors__backdrop` (line 518-520) — mobile backdrop
- `.triage-sensors-column` and related layout classes
- `.triage-sensors-aside*` classes
- All `@media (max-width: 768px)` rules for the above mobile chrome classes
- `.triage-session-info*` and `.triage-cancel-btn` classes
  </action>
  <verify>
    <automated>cd /home/victor/Git/triax-prototype && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ProtocolTriage.jsx contains `import SensorPanel from './SensorPanel'`
    - ProtocolTriage.jsx does NOT contain `const SENSOR_CONFIG = {`
    - ProtocolTriage.jsx does NOT contain `const PainInput = (`
    - ProtocolTriage.jsx does NOT contain `const GCSInput = (`
    - ProtocolTriage.jsx does NOT contain `const SensorLabel = (`
    - ProtocolTriage.jsx contains `<SensorPanel` (at least 2 occurrences — desktop and mobile)
    - ProtocolTriage.jsx contains `sensorInputs={sensorInputs}` (prop passed to SensorPanel)
    - ProtocolTriage.jsx contains `onSensorChange={handleSensorChange}` (prop passed to SensorPanel)
    - ProtocolTriage.jsx contains `onSubmit={handleSendSensors}` (prop passed to SensorPanel)
    - ProtocolTriage.jsx still contains `const [sensorInputs, setSensorInputs]` (state stays in parent)
    - ProtocolTriage.jsx still contains `const handleSendSensors` (handler stays in parent)
    - ProtocolTriage.jsx still contains `const [missingSensors, setMissingSensors]` (state stays in parent)
    - ProtocolTriage.jsx still contains `triage-session-info` (session info card stays per D-02)
    - ProtocolTriage.jsx still contains `triage-sensors-aside` (mobile container stays per D-03)
    - ProtocolTriage.jsx still contains `triage-sensors__toggle` (mobile toggle stays per D-03)
    - ProtocolTriage.css does NOT contain `.triage-sensors__submit-btn {`
    - ProtocolTriage.css does NOT contain `.triage-sensors__gcs-select {`
    - ProtocolTriage.css does NOT contain `.triage-sensors__pain-group {`
    - ProtocolTriage.css still contains `.triage-sensors-aside {` (mobile container stays)
    - ProtocolTriage.css still contains `.triage-sensors__toggle {` (mobile toggle stays)
    - ProtocolTriage.css still contains `.triage-cancel-btn {` (cancel button stays)
    - `npx vite build` completes without errors
  </acceptance_criteria>
  <done>ProtocolTriage.jsx renders SensorPanel in both desktop sidebar and mobile aside. All sensor-specific code (SENSOR_CONFIG, PainInput, GCSInput, SensorLabel, sensor CSS) lives exclusively in SensorPanel.jsx/css. Build passes with zero errors. Mobile chrome (toggle, backdrop, summary strip, session info) remains in ProtocolTriage.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes with zero errors — no broken imports, no missing components
2. `grep -c '<SensorPanel' src/components/ProtocolTriage.jsx` returns 2 (desktop + mobile)
3. `grep -c 'SENSOR_CONFIG' src/components/SensorPanel.jsx` returns at least 1; `grep -c 'SENSOR_CONFIG' src/components/ProtocolTriage.jsx` returns 0
4. `grep -c 'PainInput' src/components/SensorPanel.jsx` returns at least 1; `grep -c 'PainInput' src/components/ProtocolTriage.jsx` returns 0
5. `grep 'getFieldStatus' src/components/SensorPanel.jsx` shows the prop in destructuring and usage
6. `grep 'data-field-status' src/components/SensorPanel.jsx` shows the attribute assignment
7. `grep 'triage-sensors__submit-btn' src/components/ProtocolTriage.css` returns nothing (moved to SensorPanel.css)
8. `grep 'triage-sensors-aside' src/components/ProtocolTriage.css` returns matches (stays in ProtocolTriage.css)
</verification>

<success_criteria>
- SensorPanel.jsx is a standalone component that renders the "Sinais Vitais" card with all 8 vital sign inputs
- ProtocolTriage.jsx renders `<SensorPanel>` twice — once in the desktop sidebar, once in the mobile aside
- Zero sensor rendering logic duplicated between desktop and mobile
- Build passes, no runtime errors
- getFieldStatus callback prop is wired for Phase 12 forward-compatibility (D-06, D-07)
- Mobile BP inputs gain type/maxLength/min/max validation via unification (D-08)
- Session info card and mobile chrome (toggle, backdrop, summary) remain in ProtocolTriage (D-02, D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/10-sensor-panel-refactor/10-01-SUMMARY.md`
</output>
